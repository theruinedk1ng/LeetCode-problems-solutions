292. Nim Game

```py
class Solution:
    def canWinNim(self, n: int) -> bool:

        if n % 4 == 0:
            return False
        
        if n <= 3 or n % 5 == 0:
            return True
        
        return True
```

```js
/**
 * @param {number} n
 * @return {boolean}
 */
var canWinNim = function(n) {

    if(n % 4 === 0)
        return false
    
    if(n <= 3 || n % 5 === 0)
        return true
    
    return true
    
};
```
